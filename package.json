{
  "name": "dex-challenge",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "bootstrap": "^4.6.1",
    "gh-pages": "^2.2.0",
    "node-sass": "^7.0.0",
    "prop-types": "^15.8.1",
    "react": "^16.14.0",
    "react-bootstrap": "^1.6.4",
    "react-dom": "^16.14.0",
    "react-scripts": "3.0.0",
    "request-promise": "^4.2.6"
  },
  "homepage": "https://www.dex-challenge.site",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "pokedex-promise-v2": "^3.3.2",
    "prettier": "^1.19.1",
    "request": "^2.88.2"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,css,json,md}": [
      "prettier --write",
      "git add"
    ]
  }
}
